//Funções anonimas não recebem nome, veja o exemplo abaixo:
//OBS: UMA VARIÁVEL (LET, CONST OU VAR) PODE ARMAZENAR UMA FUNÇÃO.

const soma = function(a, b){
  return a + b; 
}

soma(2, 3) // 5
soma(5, 2) // 7

